# Tech stack: {{slug}}

> Generated {{date}} from the `{{templateName}}` template.

## Detected languages

{{detectedLanguages}}

## Pinned dependencies

<!-- @auto:dependencies -->

| Library | Version | Purpose |
|---|---|---|
| (auto-populated by `init --mode auto` from package.json / pyproject.toml / Cargo.toml / go.mod) | | |

<!-- /@auto -->

## Entry points

<!-- @auto:entry-points -->

(`init --mode auto` populates this with the heuristic entry-point
files: `src/index.ts`, `cmd/main.go`, `app/main.py`, etc.)

<!-- /@auto -->
